version: "2"
services:
  node:
    image: "node:9.11"
    user: "node"
    working_dir: /app
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
    expose:
      - "8080"
    command: "npm start"
